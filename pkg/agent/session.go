package agent

// SessionRequest holds input for the session endpoint.
type SessionRequest struct {
	// TODO: add fields (cookies, headers, etc.).
}

// SessionResponse holds the session endpoint response.
type SessionResponse struct {
	// TODO: add fields (principal, session status, etc.).
}
