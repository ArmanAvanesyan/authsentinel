package agent

// LogoutRequest holds session identifiers and redirect target for logout.
type LogoutRequest struct {
	// TODO: add fields (session identifiers, redirect target, etc.).
}

// LogoutResponse holds cleared cookies and redirect after logout.
type LogoutResponse struct {
	// TODO: add fields (cleared cookies, redirect, etc.).
}
