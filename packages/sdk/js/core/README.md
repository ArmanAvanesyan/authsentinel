# @authsentinel/sdk-core

Shared browser/client runtime for AuthSentinel SDKs.

Provides primitives for session, login, refresh, and callback handling that higher-level frameworks (React, Flutter, etc.) can build on.

