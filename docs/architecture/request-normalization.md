# Request normalization

Request normalization logic lives in `authsentinel-proxy` and the protocol adapters (`authsentinel-graphql`, `authsentinel-grpc`) and produces the proxy `Request` model consumed by the policy engine.***
