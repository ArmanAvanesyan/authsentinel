{
  "name": "authsentinel",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "release": "changeset version && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.8",
    "typescript": "^5.6.0",
    "eslint": "^9.0.0"
  }
}

